import { RedHeadingDirective } from './red-heading.directive';

describe('RedHeadingDirective', () => {
  it('should create an instance', () => {
    const directive = new RedHeadingDirective();
    expect(directive).toBeTruthy();
  });
});
